// The ONE line you'll change as you move environments.
//
// - Local dev in a desktop browser: http://127.0.0.1:8000
// - Testing on a real Android device/emulator during development: your
//   computer's LAN IP instead of 127.0.0.1, e.g. http://192.168.1.23:8000
//   ("localhost" from inside the phone means the phone itself, not your PC)
// - After you deploy the backend (Phase 7, e.g. to Render/Fly.io): its
//   public https:// URL. This is what ships inside the real APK.
const API_BASE = "http://127.0.0.1:8000";
